<VirtualHost *:80>
    ServerName localhost

    # HTML 파일이 위치할 기본 경로 지정
    DocumentRoot "/usr/local/apache2/htdocs"

    # /api/ 경로로 들어오는 요청을 파이썬 백엔드로 전달 (리버스 프록시)
    ProxyPass /api/ http://domain1-backend:8000/api/
    ProxyPassReverse /api/ http://domain1-backend:8000/api/
</VirtualHost>